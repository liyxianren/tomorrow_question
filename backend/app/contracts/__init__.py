"""Shared backend contracts locked by the controller agent.

These files define the public data shapes and transport shells that subagents
must treat as read-only unless explicitly reassigned by the controller.
"""
