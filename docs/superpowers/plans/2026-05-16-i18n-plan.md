# i18n Internationalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add comprehensive i18n support (en/zh) across frontend UI, backend presentation config, and API messages using react-i18next.

**Architecture:** react-i18next with namespaced JSON dictionaries (common/game/lobby/room/pages), i18next-browser-languagedetector for auto-detection, and backend Accept-Language header-based language selection. Existing label functions (labels.ts, panelGlossary.ts) retain signatures but delegate to i18next internally, minimizing caller changes.

**Tech Stack:** react-i18next, i18next-browser-languagedetector, i18next (core)

---

### Task 1: Install i18n Dependencies

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: Install packages**

Run: `cd frontend && npm install react-i18next i18next i18next-browser-languagedetector`
Expected: packages installed and added to package.json

- [ ] **Step 2: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore: add react-i18next and i18next-browser-languagedetector"
```

---

### Task 2: Create i18n Infrastructure

**Files:**
- Create: `frontend/src/i18n/index.ts`

- [ ] **Step 1: Create i18n initialization file**

Write `frontend/src/i18n/index.ts`:

```typescript
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import LanguageDetector from "i18next-browser-languagedetector";

import enCommon from "./resources/en/common.json";
import enGame from "./resources/en/game.json";
import enLobby from "./resources/en/lobby.json";
import enRoom from "./resources/en/room.json";
import enPages from "./resources/en/pages.json";
import zhCommon from "./resources/zh/common.json";
import zhGame from "./resources/zh/game.json";
import zhLobby from "./resources/zh/lobby.json";
import zhRoom from "./resources/zh/room.json";
import zhPages from "./resources/zh/pages.json";

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { common: enCommon, game: enGame, lobby: enLobby, room: enRoom, pages: enPages },
      zh: { common: zhCommon, game: zhGame, lobby: zhLobby, room: zhRoom, pages: zhPages },
    },
    fallbackLng: "en",
    defaultNS: "common",
    ns: ["common", "game", "lobby", "room", "pages"],
    detection: {
      order: ["localStorage", "navigator"],
      caches: ["localStorage"],
      lookupLocalStorage: "app_locale",
    },
    interpolation: { escapeValue: false },
  });

export default i18n;
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/i18n/index.ts
git commit -m "feat: create i18n initialization with react-i18next"
```

---

### Task 3: Create Game Namespace Dictionaries (en + zh)

**Files:**
- Create: `frontend/src/i18n/resources/en/game.json`
- Create: `frontend/src/i18n/resources/zh/game.json`

- [ ] **Step 1: Create English game.json**

Write `frontend/src/i18n/resources/en/game.json`:

```json
{
  "phase": {
    "decision": "National Decision",
    "market": "Market Sales",
    "settlement": "Fiscal Settlement",
    "production": "Production",
    "military": "Military",
    "politics": "Politics"
  },
  "country": {
    "austria": "Austria",
    "britain": "Britain",
    "france": "France",
    "prussia": "Prussia",
    "russia": "Russia"
  },
  "productionRoute": {
    "idle": "Idle Capacity",
    "handicraft": "Handicraft",
    "mechanized": "Mechanized Industry",
    "steam": "Steam Industry",
    "electrified": "Electrified Industry"
  },
  "goods": {
    "phase1_goods": "Unified Goods"
  },
  "accessLevel": {
    "closed": "Closed",
    "colony": "Colonial Control",
    "concession": "Concession",
    "open": "Open Trade"
  },
  "ideology": {
    "conservatism": "Conservatism",
    "egalitarianism": "Egalitarianism",
    "liberalism": "Liberalism",
    "monarchy": "Monarchism",
    "nationalism": "Nationalism",
    "republicanism": "Republicanism",
    "socialism": "Socialism"
  },
  "reform": {
    "civil service": "Civil Service",
    "factory act": "Factory Act",
    "land reform": "Land Reform",
    "public schools": "Public Schools"
  },
  "policy": {
    "free trade": "Free Trade",
    "naval act": "Naval Act",
    "press controls": "Press Controls",
    "protective tariffs": "Protective Tariffs"
  },
  "technology": {
    "spinning_jenny": "Spinning Jenny",
    "steam_engine": "Steam Engine",
    "steelmaking": "Steelmaking",
    "watt_engine": "Watt Engine",
    "lathe": "Lathe",
    "power_generation": "Power Generation",
    "combustion_engine": "Combustion Engine"
  },
  "researchFacility": {
    "academy": "Academy",
    "public_labs": "Public Labs"
  },
  "region": {
    "balkans": "Balkans",
    "india": "India",
    "europe": "Europe",
    "americas": "Americas",
    "africa": "Africa",
    "middle_east": "Middle East",
    "asia_pacific": "Asia-Pacific"
  },
  "oceanNode": {
    "mediterranean": "Mediterranean",
    "north-sea": "North Sea"
  },
  "route": {
    "atlantic": "Atlantic",
    "baltic": "Baltic",
    "suez": "Suez"
  },
  "unit": {
    "artillery": "Artillery",
    "cavalry": "Cavalry",
    "frigates": "Frigates",
    "infantry": "Infantry",
    "ironclads": "Ironclads"
  },
  "socketState": {
    "idle": "Awaiting Connection",
    "connecting": "Connecting",
    "connected": "Connected",
    "disconnected": "Disconnected"
  }
}
```

- [ ] **Step 2: Create Chinese game.json**

Write `frontend/src/i18n/resources/zh/game.json`:

```json
{
  "phase": {
    "decision": "国家决策",
    "market": "市场出售",
    "settlement": "财政结算",
    "production": "生产",
    "military": "军事",
    "politics": "政治"
  },
  "country": {
    "austria": "奥地利",
    "britain": "英国",
    "france": "法国",
    "prussia": "普鲁士",
    "russia": "俄罗斯"
  },
  "productionRoute": {
    "idle": "空置产能",
    "handicraft": "手工业",
    "mechanized": "机械化工业",
    "steam": "蒸汽工业",
    "electrified": "电气工业"
  },
  "goods": {
    "phase1_goods": "统一商品"
  },
  "accessLevel": {
    "closed": "对外关闭",
    "colony": "殖民控制",
    "concession": "特许经营",
    "open": "开放贸易"
  },
  "ideology": {
    "conservatism": "保守主义",
    "egalitarianism": "平等主义",
    "liberalism": "自由主义",
    "monarchy": "君主主义",
    "nationalism": "民族主义",
    "republicanism": "共和主义",
    "socialism": "社会主义"
  },
  "reform": {
    "civil service": "文官制度",
    "factory act": "工厂法",
    "land reform": "土地改革",
    "public schools": "公立学校"
  },
  "policy": {
    "free trade": "自由贸易",
    "naval act": "海军法",
    "press controls": "新闻管制",
    "protective tariffs": "保护性关税"
  },
  "technology": {
    "spinning_jenny": "珍妮纺纱机",
    "steam_engine": "蒸汽机",
    "steelmaking": "炼钢法",
    "watt_engine": "瓦特蒸汽机",
    "lathe": "车床",
    "power_generation": "发电",
    "combustion_engine": "内燃机"
  },
  "researchFacility": {
    "academy": "学院",
    "public_labs": "公共实验室"
  },
  "region": {
    "balkans": "巴尔干",
    "india": "印度",
    "europe": "欧洲",
    "americas": "美洲",
    "africa": "非洲",
    "middle_east": "中东",
    "asia_pacific": "亚太"
  },
  "oceanNode": {
    "mediterranean": "地中海",
    "north-sea": "北海"
  },
  "route": {
    "atlantic": "大西洋",
    "baltic": "波罗的海",
    "suez": "苏伊士"
  },
  "unit": {
    "artillery": "火炮",
    "cavalry": "骑兵",
    "frigates": "护卫舰",
    "infantry": "步兵",
    "ironclads": "铁甲舰"
  },
  "socketState": {
    "idle": "等待连接",
    "connecting": "连接中",
    "connected": "已连接",
    "disconnected": "已断开"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/resources/en/game.json frontend/src/i18n/resources/zh/game.json
git commit -m "feat: add game namespace i18n dictionaries (en/zh)"
```

---

### Task 4: Update labels.ts and panelGlossary.ts to Use i18next

**Files:**
- Modify: `frontend/src/features/game/labels.ts`
- Modify: `frontend/src/features/game/panelGlossary.ts`

- [ ] **Step 1: Update labels.ts**

Replace content of `frontend/src/features/game/labels.ts`:

```typescript
import i18n from "../../i18n";
import { getCountryLabel as getPanelCountryLabel } from "./panelGlossary";
import type { FrontendSocketState } from "./runtime/types";
import type { GamePhase } from "../../types";

export function getPhaseLabel(phase: GamePhase | string): string {
  return i18n.t("game:phase." + phase, { defaultValue: String(phase) });
}

export function getCountryLabel(country: string | null): string {
  return getPanelCountryLabel(country);
}

export function formatSeconds(value: number | null): string {
  if (value === null) {
    return "--:--";
  }
  const minutes = Math.floor(value / 60)
    .toString()
    .padStart(2, "0");
  const seconds = Math.max(0, value % 60)
    .toString()
    .padStart(2, "0");

  return `${minutes}:${seconds}`;
}

export function getSocketStateLabel(socketState: FrontendSocketState): string {
  return i18n.t("game:socketState." + socketState, { defaultValue: socketState });
}
```

- [ ] **Step 2: Update panelGlossary.ts**

Replace content of `frontend/src/features/game/panelGlossary.ts`:

```typescript
import i18n from "../../i18n";

function humanizeKey(value: unknown): string {
  return String(value ?? "")
    .replace(/[-_]/g, " ")
    .replace(/\b\w/g, (char) => char.toUpperCase());
}

function resolveLabel(ns: string, value: string): string {
  if (typeof value !== "string") {
    return humanizeKey(value);
  }
  return i18n.t(`game:${ns}.${value}`, { defaultValue: humanizeKey(value) });
}

const reformReverseLookup: Record<string, string> = {
  "文官制度": "civil service",
  "工厂法": "factory act",
  "土地改革": "land reform",
  "公立学校": "public schools",
};

const policyReverseLookup: Record<string, string> = {
  "自由贸易": "free trade",
  "海军法": "naval act",
  "新闻管制": "press controls",
  "保护性关税": "protective tariffs",
};

export function getCountryLabel(value: string | null | undefined): string {
  if (!value) return i18n.t("common:none", "None");
  return resolveLabel("country", value);
}

export function getProductionRouteLabel(value: string): string {
  return resolveLabel("productionRoute", value);
}

export function getGoodsLabel(value: string): string {
  return resolveLabel("goods", value);
}

export function getAccessLevelLabel(value: string): string {
  return resolveLabel("accessLevel", value);
}

export function getIdeologyLabel(value: string): string {
  return resolveLabel("ideology", value);
}

export function getReformLabel(value: string): string {
  return resolveLabel("reform", value);
}

export function getPolicyLabel(value: string): string {
  return resolveLabel("policy", value);
}

export function getTechnologyLabel(value: string): string {
  return resolveLabel("technology", value);
}

export function getResearchFacilityLabel(value: string): string {
  return resolveLabel("researchFacility", value);
}

export function getRegionLabel(value: string): string {
  return resolveLabel("region", value);
}

export function getOceanNodeLabel(value: string): string {
  return resolveLabel("oceanNode", value);
}

export function getRouteLabel(value: string): string {
  return resolveLabel("route", value);
}

export function getUnitLabel(value: string): string {
  return resolveLabel("unit", value);
}

export function formatTranslatedAgenda(
  items: string[],
  getLabel: (value: string) => string,
): string {
  return items.map((item) => getLabel(item)).join("\n");
}

export function resolveReformKey(value: string): string {
  return reformReverseLookup[value] ?? value.trim();
}

export function resolvePolicyKey(value: string): string {
  return policyReverseLookup[value] ?? value.trim();
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/features/game/labels.ts frontend/src/features/game/panelGlossary.ts
git commit -m "refactor: delegate labels and panelGlossary to i18next"
```

---

### Task 5: Create Common Namespace Dictionaries (en + zh)

**Files:**
- Create: `frontend/src/i18n/resources/en/common.json`
- Create: `frontend/src/i18n/resources/zh/common.json`

- [ ] **Step 1: Create English common.json**

Write `frontend/src/i18n/resources/en/common.json`:

```json
{
  "none": "None",
  "loading": "Loading...",
  "save": "Save",
  "saving": "Saving...",
  "cancel": "Cancel",
  "backToLobby": "Back to Lobby",
  "backToHome": "Back to Home",
  "enterLobby": "Enter Lobby",
  "settings": "Settings",
  "notFound": "404 - Page Not Found",
  "notFoundHint": "The page you are looking for does not exist.",
  "loadFailed": "Failed to load",
  "saveSuccess": "Saved successfully",
  "saveFailed": "Save failed",
  "backendUnavailable": "Backend service unavailable. Please confirm the local API is running."
}
```

- [ ] **Step 2: Create Chinese common.json**

Write `frontend/src/i18n/resources/zh/common.json`:

```json
{
  "none": "无",
  "loading": "正在加载...",
  "save": "保存",
  "saving": "保存中...",
  "cancel": "取消",
  "backToLobby": "返回大厅",
  "backToHome": "返回首页",
  "enterLobby": "进入大厅",
  "settings": "设置",
  "notFound": "404 - 页面未找到",
  "notFoundHint": "你访问的页面不存在。",
  "loadFailed": "加载失败",
  "saveSuccess": "保存成功",
  "saveFailed": "保存失败",
  "backendUnavailable": "后端服务不可用，请确认本地 API 已启动。"
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/resources/en/common.json frontend/src/i18n/resources/zh/common.json
git commit -m "feat: add common namespace i18n dictionaries (en/zh)"
```

---

### Task 6: Update main.tsx and http.ts (Accept-Language Header)

**Files:**
- Modify: `frontend/src/main.tsx`
- Modify: `frontend/src/services/http.ts`

- [ ] **Step 1: Update main.tsx to import i18n**

Add before the `ReactDOM.createRoot` call in `frontend/src/main.tsx`:

```typescript
import "./i18n";
```

The file should become:

```typescript
import React from "react";
import ReactDOM from "react-dom/client";

import { App } from "./app/App";
import "./i18n";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 2: Add Accept-Language header in http.ts**

In `frontend/src/services/http.ts`, add an import at the top:

```typescript
import i18n from "../i18n";
```

Then in `runApiRequest`, after `requestHeaders.set("Content-Type", "application/json")`, add:

```typescript
requestHeaders.set("Accept-Language", i18n.language);
```

- [ ] **Step 3: Convert hardcoded Chinese error message in http.ts**

Change line 166:
```typescript
"后端服务不可用，请确认本地 API 已启动。",
```
to:
```typescript
i18n.t("common:backendUnavailable", "Backend service unavailable. Please confirm the local API is running."),
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/main.tsx frontend/src/services/http.ts
git commit -m "feat: wire i18n into app entry and Accept-Language header"
```

---

### Task 7: Create Pages Namespace Dictionaries (en + zh)

**Files:**
- Create: `frontend/src/i18n/resources/en/pages.json`
- Create: `frontend/src/i18n/resources/zh/pages.json`

- [ ] **Step 1: Create English pages.json**

Write `frontend/src/i18n/resources/en/pages.json`:

```json
{
  "home": {
    "eyebrow": "5-Player Turn-Based Great Power Strategy",
    "title": "Jump right in, even on your first game",
    "description": "A 5-player turn-based great power management game. Each turn you make limited choices across three phases—National Decision, Market Sales, and Fiscal Settlement—see the results roll into the next round, and compete for the highest cumulative total income.",
    "badges": {
      "multiplayer": "Multiplayer Strategy",
      "turns": "15 Rounds",
      "threePhase": "Three-Phase Agenda"
    },
    "heroActions": {
      "enterLobby": "Enter Lobby",
      "rule1": "5-Player Fixed Game",
      "rule2": "Pick a Nation",
      "rule3": "All Ready Up",
      "rule4": "Auto Start"
    },
    "asideCard": {
      "eyebrow": "You only need to know 4 steps before entering the lobby",
      "title": "4 Steps to Your First Game",
      "description": "No need to read long rules upfront. The system guides you through identity confirmation, room preparation, and auto-start.",
      "steps": [
        "Confirm your nickname—the system uses it as your identity on this device.",
        "Enter the lobby, then create a room or join one that's waiting.",
        "After entering a room, pick a nation to represent in this match.",
        "Once everyone clicks ready, the system auto-starts Round 1."
      ]
    },
    "cycleCard": {
      "eyebrow": "How to Play",
      "title": "The Three-Phase Cycle",
      "description": "You won't need to memorize a wall of rules. Just follow this fixed cycle each round—results from one phase flow into the next round's operations.",
      "phases": {
        "decision": {
          "title": "National Decision",
          "desc": "Allocate budgets across factories, government policies, market previews, military, and research—setting the stage for the phases ahead."
        },
        "market": {
          "title": "Market Sales",
          "desc": "Sell goods on domestic and overseas markets. Supply and demand determine prices; sales convert directly into national income."
        },
        "settlement": {
          "title": "Fiscal Settlement",
          "desc": "Aggregate income and redistribute it proportionally into consumer purchasing power, factories, and government finance—rolling into the next round."
        }
      }
    },
    "lobbyCard": {
      "eyebrow": "What Happens in the Lobby",
      "title": "Your Path to a Room",
      "description": "The home page tells you what this game is. Creating rooms, picking games, and resuming progress all happen in the lobby.",
      "steps": [
        "Confirm your nickname, then follow the lobby's guided flow.",
        "The lobby shows which rooms are waiting for players and how to resume your last session.",
        "Join a room, pick your nation, click Ready. The game starts automatically when everyone's ready."
      ]
    }
  },
  "settings": {
    "title": "Settings",
    "eyebrow": "Parameter Panel",
    "numericConfigTitle": "Numeric Configuration",
    "numericConfigDescription": "Above are common balance items; the bottom \"All JSON Numeric Values\" section auto-lists remaining numbers from config files. Saving writes back to backend/config/balance/*.json and clears the config cache.",
    "saveButton": "Save",
    "savingButton": "Saving...",
    "allJsonValuesTitle": "All JSON Numeric Values",
    "allJsonValuesEyebrow": "balance/*.json",
    "allJsonValuesDescription": "Lists all numeric values not shown in the dedicated panels above. Chinese names are for reference; the gray path indicates the JSON location. Modify and click Save at the top to apply.",
    "loadErrorTitle": "Settings Panel",
    "loadErrorDescription": "Failed to load settings",
    "productionCost": {
      "title": "Production Costs",
      "eyebrow": "production",
      "routeColumn": "Production Method",
      "newCostColumn": "New Factory Cost",
      "upgradeCostColumn": "Upgrade Cost"
    },
    "rawMaterials": {
      "title": "Country Raw Materials",
      "eyebrow": "countries",
      "countryColumn": "Country",
      "initialColumn": "Initial Raw Materials",
      "perTurnColumn": "Per-Turn Increase"
    },
    "market": {
      "title": "Market Parameters",
      "eyebrow": "market",
      "baseIncomeLabel": "baseIncomePerRound (Guaranteed Income per Round)",
      "regionColumn": "Region",
      "priceMultiplierColumn": "Price Multiplier"
    },
    "government": {
      "title": "Government Parameters",
      "eyebrow": "government",
      "adminCostLabel": "Administration Cost",
      "ideologyMinLabel": "Ideology Range - Min",
      "ideologyMaxLabel": "Max",
      "thresholdColumns": {
        "ideology": "Ideology",
        "highThreshold": "High Threshold",
        "lowThreshold": "Low Threshold"
      }
    },
    "jsonValueColumns": {
      "name": "Name",
      "value": "Value"
    },
    "configFiles": {
      "abilities.json": "National Abilities",
      "countries.json": "Country Initial Values",
      "decision_actions.json": "Actions / Policies",
      "events.json": "Events",
      "global.json": "Global",
      "market.json": "Market",
      "military.json": "Military",
      "military_actions.json": "Military Actions",
      "politics.json": "Politics / Ideologies",
      "production.json": "Production",
      "reforms.json": "Reforms",
      "regions.json": "Regions",
      "research_actions.json": "Tech Tree",
      "technology.json": "Technology Tree"
    }
  },
  "notFound": {
    "title": "Page Not Found",
    "hint": "The page you're looking for doesn't exist or may have been moved.",
    "action": "Back to Home"
  },
  "settlement": {
    "title": "Fiscal Settlement",
    "loading": "Loading settlement...",
    "noData": "No settlement data available."
  }
}
```

- [ ] **Step 2: Create Chinese pages.json**

Write `frontend/src/i18n/resources/zh/pages.json` with the original Chinese text (transferred from existing hardcoded values). This will be a large file containing all the original Chinese strings mapped to the same keys. Key mappings mirror the English file exactly.

```json
{
  "home": {
    "eyebrow": "5 人回合制列强经营对局",
    "title": "第一次进入也能顺着玩完一局",
    "description": "这是一局 5 人参与的回合制列强经营对局。每回合你将在国家决策、市场出售、财政结算三个阶段中做有限选择，拿结果、滚动下一轮，并最终按累计总收入排名。",
    "badges": {
      "multiplayer": "多人策略对局",
      "turns": "15 回合推进",
      "threePhase": "三阶段议程"
    },
    "heroActions": {
      "enterLobby": "进入大厅",
      "rule1": "5 人固定局",
      "rule2": "选国家",
      "rule3": "全员准备",
      "rule4": "自动开局"
    },
    "asideCard": {
      "eyebrow": "进入大厅前你只需要知道 4 步",
      "title": "4 步进入第一局",
      "description": "这里不需要先读很长规则。进入大厅后，系统会把你带进身份确认、房间准备和自动开局这条主线。",
      "steps": [
        "先确认昵称，系统会把它当作你这台设备的身份。",
        "进入大厅后，创建房间或加入等待中的房间。",
        "进入房间后选择国家，确认你这一局代表谁。",
        "全员点下准备后，系统会自动进入第 1 回合。"
      ]
    },
    "cycleCard": {
      "eyebrow": "这局怎么玩",
      "title": "三阶段经营循环",
      "description": "整局不会要求你背规则墙。你只需要知道每回合固定按这条循环推进，上一阶段的结果会回流到下一轮经营。",
      "phases": {
        "decision": {
          "title": "国家决策",
          "desc": "在工厂、政府政策、市场预览、军事和研究院中分配预算，为后续阶段排兵布阵。"
        },
        "market": {
          "title": "市场出售",
          "desc": "把商品投放到国内和海外市场，供需决定价格，销售直接转化为国家收入。"
        },
        "settlement": {
          "title": "财政结算",
          "desc": "结算阶段汇总收入并按比例回流到民间购买力、工厂和政府财政，滚动进入下一回合。"
        }
      }
    },
    "lobbyCard": {
      "eyebrow": "进入大厅后会发生什么",
      "title": "从这里进入房间主线",
      "description": "首页只负责告诉你这局是什么；真正的创建、选房和继续上次进度都放在大厅里，避免第一次进入就被表单和状态打断。",
      "steps": [
        "先确认昵称，再进入大厅的下一步引导。",
        "大厅会告诉你哪些房间正在等人，也会提示如何回到上次进度。",
        "进入房间后选择国家、点击准备，等全员满足条件后系统自动开局。"
      ]
    }
  },
  "settings": {
    "title": "设置",
    "eyebrow": "参数面板",
    "numericConfigTitle": "数值配置",
    "numericConfigDescription": "上方是常用平衡项；底部\"全部 JSON 数值\"会自动列出配置文件里的其余数字。保存后写回 backend/config/balance/*.json 并清空配置缓存。",
    "saveButton": "保存",
    "savingButton": "保存中...",
    "allJsonValuesTitle": "全部 JSON 数值",
    "allJsonValuesEyebrow": "balance/*.json",
    "allJsonValuesDescription": "这里列出未在上方常用面板中单独展示的全部数值。中文名称用于理解含义，灰色路径用于定位 JSON 位置；修改后点击顶部保存即可生效。",
    "loadErrorTitle": "配置面板",
    "loadErrorDescription": "加载失败",
    "productionCost": {
      "title": "生产成本",
      "eyebrow": "production",
      "routeColumn": "生产方式",
      "newCostColumn": "新建成本",
      "upgradeCostColumn": "升级成本"
    },
    "rawMaterials": {
      "title": "各国原材料",
      "eyebrow": "countries",
      "countryColumn": "国家",
      "initialColumn": "初始原材料",
      "perTurnColumn": "每回合增量"
    },
    "market": {
      "title": "市场参数",
      "eyebrow": "market",
      "baseIncomeLabel": "baseIncomePerRound（每回合保底收入）",
      "regionColumn": "区域",
      "priceMultiplierColumn": "价格倍率"
    },
    "government": {
      "title": "政府参数",
      "eyebrow": "government",
      "adminCostLabel": "行政能力价格（administrationCost）",
      "ideologyMinLabel": "思潮范围 - 最小值",
      "ideologyMaxLabel": "最大值",
      "thresholdColumns": {
        "ideology": "思潮",
        "highThreshold": "高阈值",
        "lowThreshold": "低阈值"
      }
    },
    "jsonValueColumns": {
      "name": "中文名称",
      "value": "数值"
    },
    "configFiles": {
      "abilities.json": "国家能力",
      "countries.json": "国家初始值",
      "decision_actions.json": "行动 / 政策",
      "events.json": "事件",
      "global.json": "全局",
      "market.json": "市场",
      "military.json": "军事",
      "military_actions.json": "军事行动",
      "politics.json": "政治 / 思潮",
      "production.json": "生产",
      "reforms.json": "改革",
      "regions.json": "区域",
      "research_actions.json": "天赋树",
      "technology.json": "科技树"
    }
  },
  "notFound": {
    "title": "页面未找到",
    "hint": "你访问的页面不存在或可能已被移动。",
    "action": "返回首页"
  },
  "settlement": {
    "title": "财政结算",
    "loading": "正在加载结算...",
    "noData": "暂无结算数据。"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/resources/en/pages.json frontend/src/i18n/resources/zh/pages.json
git commit -m "feat: add pages namespace i18n dictionaries (en/zh)"
```

---

### Task 8: Create Lobby Namespace Dictionaries (en + zh)

**Files:**
- Create: `frontend/src/i18n/resources/en/lobby.json`
- Create: `frontend/src/i18n/resources/zh/lobby.json`

- [ ] **Step 1: Create English lobby.json**

Write `frontend/src/i18n/resources/en/lobby.json`:

```json
{
  "eyebrow": "Room Lobby",
  "title": "Create or Join a Game",
  "lead": "Confirm your display name first, then join from the available rooms. Create a new room only when none fit. Use a room code only if you received an invite.",
  "flow": {
    "step1": "Set Nickname",
    "step2": "Create or Join",
    "step3": "Pick Nation",
    "step4": "Ready Up"
  },
  "identityCard": {
    "noProfileDisplayName": "No Nickname Set",
    "noProfileHelperText": "Set a nickname before creating or joining rooms on this device.",
    "profileIdLabel": "Device ID",
    "noProfileIdValue": "Auto-generated after setting nickname",
    "actionSet": "Set Nickname",
    "hasProfileHelperText": "This nickname will be used when creating or joining rooms.",
    "actionChange": "Change Nickname",
    "notBoundName": "No Display Name Bound",
    "notBoundIdValue": "Auto-generated after identity confirmation",
    "clearIdentity": "Clear Identity",
    "description": "Resume a room or game you left earlier.",
    "sessionStateLabel": "Saved Progress",
    "noProgress": "No saved progress to resume.",
    "hasProgressPrefix": "This device has saved progress: ",
    "restoreLabel": "Resume Progress",
    "restoringLabel": "Resuming...",
    "clearLabel": "Clear Record"
  },
  "roomsSection": {
    "title": "Available Rooms",
    "refresh": "Refresh",
    "loading": "Loading rooms...",
    "empty": "No rooms waiting for players right now.",
    "emptyHint": "You can create a new room on the right."
  },
  "entryForm": {
    "createTitle": "Create New Room",
    "createDescription": "Create a game and invite other players to join.",
    "joinTitle": "Join with Room Code",
    "joinDescription": "Enter a room code from a friend to join a private room.",
    "roomCodePlaceholder": "e.g. ABCD-1234",
    "createButton": "Create Room",
    "joinButton": "Join Room",
    "inviteDescriptionWithCode": "A friend shared a room code with you—confirm your identity to join {code}.",
    "inviteDescriptionPrefilled": "Room code {code} is already filled in—confirm your identity to join.",
    "inviteJoinButton": "Join {code}"
  },
  "roomCard": {
    "hostLabel": "Host {name}",
    "members": "{count} / {max} players",
    "availableSeats": "{count} seats open",
    "readyCount": "Ready {ready} / {total}",
    "selectedCountries": "{count} / {max} nations selected",
    "statusJoinable": "Available to Join",
    "statusUnavailable": "Not Available",
    "activityRecent": "Recent Activity",
    "activityWaiting": "Waiting for Refresh",
    "joinLabel": "Join {code}",
    "waitingHost": "Waiting for host info"
  },
  "recentRoomCard": {
    "statusWaiting": "Waiting to Start",
    "statusInGame": "In Progress",
    "statusFinished": "Finished",
    "statusUnknown": "Status Pending",
    "statusUnavailable": "Temporarily Unavailable",
    "memberCountUnavailable": "Member info unavailable",
    "unavailableDetail": "This entry is temporarily unavailable. You can still rejoin by room code.",
    "retryLabel": "Try Again Later",
    "membersLabel": "{count} / 5 players",
    "detailInGame": "When you left, this room had entered a game or finished settlement.",
    "detailWaiting": "You can return to the room to continue preparation or wait for the game to start.",
    "actionViewSettlement": "View Results",
    "actionBackToGame": "Back to Game",
    "actionEnterRoom": "Enter Room"
  },
  "continueBanner": {
    "title": "Recoverable Progress",
    "description": "Found a room or game you left earlier.",
    "actionLabel": "Resume"
  },
  "statusNotice": {
    "creating": "Creating room...",
    "joining": "Joining room..."
  }
}
```

- [ ] **Step 2: Create Chinese lobby.json**

Write `frontend/src/i18n/resources/zh/lobby.json` with the original Chinese text matching the same key structure.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/resources/en/lobby.json frontend/src/i18n/resources/zh/lobby.json
git commit -m "feat: add lobby namespace i18n dictionaries (en/zh)"
```

---

### Task 9: Update Lobby ViewModel and Components

**Files:**
- Modify: `frontend/src/features/lobby/flow/viewModel.ts`
- Modify: `frontend/src/features/lobby/flow/model.ts`
- Modify: `frontend/src/features/lobby/flow/useIdentityGateController.ts`
- Modify: `frontend/src/features/lobby/flow/useLobbyFlowController.ts`
- Modify: `frontend/src/components/lobby/*.tsx` (6 files)

- [ ] **Step 1: Update viewModel.ts**

Replace all hardcoded Chinese strings with `i18n.t()` calls. Import `i18n` from `"../../i18n"` at the top.

Key replacements:
- `mapRoomStatus`: `"等待开局"` → `i18n.t("lobby:recentRoomCard.statusWaiting")`, `"进行中"` → `i18n.t("lobby:recentRoomCard.statusInGame")`, `"已结束"` → `i18n.t("lobby:recentRoomCard.statusFinished")`, `"状态待确认"` → `i18n.t("lobby:recentRoomCard.statusUnknown")`
- `COUNTRY_LABEL_BY_CODE` map → use `i18n.t("game:country.xxx")`
- `buildCurrentIdentityCardViewModel`: all strings → `i18n.t("lobby:identityCard.xxx")`
- `buildRecoverableSessionViewModel`: all strings → `i18n.t("lobby:identityCard.xxx")`
- `buildWaitingRoomCardViewModel`: all strings → `i18n.t("lobby:roomCard.xxx")`
- `buildLobbyPrimaryActionViewModel`: all strings → `i18n.t("lobby:entryForm.xxx")`
- `buildInviteEntryViewModel`: all strings → `i18n.t("lobby:entryForm.xxx")`
- `buildRecoverableGameBannerViewModel`: all strings → `i18n.t("lobby:continueBanner.xxx")` / `i18n.t("lobby:recentRoomCard.xxx")`
- `buildUnavailableRecentRoomCard`: all strings → `i18n.t("lobby:recentRoomCard.xxx")`
- `buildRecentRoomCard`: all strings → `i18n.t("lobby:recentRoomCard.xxx")`

- [ ] **Step 2: Update model.ts**

Replace hardcoded Chinese strings with i18n calls. Import `i18n` from `"../../i18n"`.

- [ ] **Step 3: Update useIdentityGateController.ts**

Replace hardcoded Chinese strings with i18n calls. Import `i18n` from `"../../i18n"`.

- [ ] **Step 4: Update useLobbyFlowController.ts**

Replace hardcoded Chinese strings with i18n calls. Import `i18n` from `"../../i18n"`.

- [ ] **Step 5: Update lobby components**

Replace all hardcoded Chinese text in JSX with `useTranslation()` calls in:
- `IdentityGateForm.tsx`
- `LobbyContinueBanner.tsx`
- `LobbyCurrentIdentityCard.tsx`
- `LobbyEntryForm.tsx`
- `LobbyRecoveryCard.tsx`
- `LobbyWaitingRoomsSection.tsx`

- [ ] **Step 6: Commit**

```bash
git add frontend/src/features/lobby/ frontend/src/components/lobby/
git commit -m "feat: i18n for lobby views and viewModels"
```

---

### Task 10: Create Room Namespace Dictionaries and Update Room Components

**Files:**
- Create: `frontend/src/i18n/resources/en/room.json`
- Create: `frontend/src/i18n/resources/zh/room.json`
- Modify: `frontend/src/features/room/flow/model.ts`
- Modify: `frontend/src/features/room/flow/useRoomFlowController.ts`
- Modify: `frontend/src/features/room/roomPreparationViewModel.ts`
- Modify: `frontend/src/components/room/*.tsx` (4 files)

- [ ] **Step 1: Create English room.json**

```json
{
  "eyebrow": "Game Room",
  "title": "Room {code}",
  "memberCount": "{count} / {max} players",
  "readyCount": "Ready {ready} / {total}",
  "status": {
    "waiting": "Waiting for Players",
    "readying": "Preparing to Start",
    "in_game": "Game in Progress",
    "finished": "Game Finished"
  },
  "actions": {
    "ready": "Ready",
    "unready": "Unready",
    "leave": "Leave Room",
    "start": "Start Game",
    "copyCode": "Copy Room Code",
    "codeCopied": "Copied!"
  },
  "countrySelection": {
    "title": "Select Your Nation",
    "noSelection": "No nation selected",
    "taken": "(Taken)",
    "confirm": "Confirm Selection"
  },
  "members": {
    "title": "Room Members",
    "host": "Host",
    "you": "You",
    "empty": "Waiting for players..."
  },
  "errors": {
    "roomNotFound": "Room not found",
    "roomFull": "Room is full",
    "alreadyInRoom": "You are already in this room",
    "genericError": "Something went wrong"
  }
}
```

- [ ] **Step 2: Create Chinese room.json** with corresponding Chinese strings

- [ ] **Step 3: Update room feature files and components**

Replace all Chinese text with i18n calls in the 7 room files.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/i18n/resources/en/room.json frontend/src/i18n/resources/zh/room.json frontend/src/features/room/ frontend/src/components/room/
git commit -m "feat: i18n for room views and viewModels"
```

---

### Task 11: Update Page Components (HomePage, NotFoundPage, SettingsPage, SettlementPage, IdentityPage)

**Files:**
- Modify: `frontend/src/pages/HomePage.tsx`
- Modify: `frontend/src/pages/NotFoundPage.tsx`
- Modify: `frontend/src/pages/SettingsPage.tsx`
- Modify: `frontend/src/pages/SettlementPage.tsx`
- Modify: `frontend/src/pages/IdentityPage.tsx`
- Modify: `frontend/src/pages/LobbyPage.tsx`
- Modify: `frontend/src/pages/RoomPage.tsx`
- Modify: `frontend/src/pages/GamePage.tsx`
- Modify: `frontend/src/pages/DecisionCardDemoPage.tsx`

- [ ] **Step 1: Update SettingsPage.tsx**

Replace all hardcoded Chinese with `useTranslation("pages")`:
- Labels like `"手工业 (handicraft)"` → `t("game:productionRoute.handicraft") + " (handicraft)"`
- `COUNTRY_LABELS` → use `t("game:country.xxx")`
- `REGION_LABELS` → use `t("game:region.xxx")`
- Table headers → use `t("settings.xxx.columnName")`
- Status messages → use `t("common:saveSuccess")` / `t("common:saveFailed")`
- All JSX text → use `t()` calls

- [ ] **Step 2: Update HomePage.tsx**

Replace all JSX hardcoded Chinese with `useTranslation("pages")` calls referencing keys like `t("home.eyebrow")`, `t("home.title")`, etc.

- [ ] **Step 3: Update remaining pages (NotFoundPage, SettlementPage, IdentityPage, LobbyPage, RoomPage, GamePage, DecisionCardDemoPage)**

Replace all hardcoded Chinese text with i18n calls.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/
git commit -m "feat: i18n for all page components"
```

---

### Task 12: Update Game Panel Components (Part 1 — Heavy Files)

**Files:**
- Modify: `frontend/src/components/game/panels/GamePhasePanelContent.tsx` (~109 Chinese lines)
- Modify: `frontend/src/components/game/panels/GovernmentPanel.tsx` (~197 Chinese lines)
- Modify: `frontend/src/components/game/panels/Phase1MarketPanel.tsx` (~58 Chinese lines)
- Modify: `frontend/src/components/game/panels/ResearchPanel.tsx` (~52 Chinese lines)

- [ ] **Step 1: Update each file**

For each component, add `import { useTranslation } from "react-i18next"` and use `const { t } = useTranslation()`. Replace all hardcoded Chinese strings with appropriate t() calls, referencing the game/common/pages namespaces as appropriate. Where `getXxxLabel()` functions from panelGlossary are already used, they automatically work via i18next.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/game/panels/GamePhasePanelContent.tsx frontend/src/components/game/panels/GovernmentPanel.tsx frontend/src/components/game/panels/Phase1MarketPanel.tsx frontend/src/components/game/panels/ResearchPanel.tsx
git commit -m "feat: i18n for heavy game panel components (part 1)"
```

---

### Task 13: Update Game Panel Components (Part 2)

**Files:**
- Modify: `frontend/src/components/game/panels/DomesticPanel.tsx` (~28 Chinese lines)
- Modify: `frontend/src/components/game/panels/MilitaryPanel.tsx` (~22 Chinese lines)
- Modify: `frontend/src/components/game/panels/DecisionCommandDeck.tsx` (~18 Chinese lines)
- Modify: `frontend/src/components/game/panels/factory/FactoryPanel.tsx` (~21 Chinese lines)
- Modify: `frontend/src/components/game/panels/factory/Phase1ProductionPanel.tsx` (~44 Chinese lines)
- Modify: `frontend/src/components/game/panels/military/MilitaryNodeDrawer.tsx` (~60 Chinese lines)
- Modify: `frontend/src/components/game/panels/military/MilitaryWorldMap.tsx` (~12 Chinese lines)
- Modify: `frontend/src/components/game/panels/DecisionResourceBar.tsx` (~5 Chinese lines)
- Modify: `frontend/src/components/game/panels/DecisionStepTabs.tsx` (~6 Chinese lines)
- Modify: `frontend/src/components/game/panels/shared/DecisionActionCard.tsx` (~5 Chinese lines)

- [ ] **Step 1: Update each file with i18n**

Replace all hardcoded Chinese text with `t()` calls in each component. Most of these use labels/lookups already going through panelGlossary functions, so changes may be limited to UI wrapper text, button labels, and status messages.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/game/panels/
git commit -m "feat: i18n for game panel components (part 2)"
```

---

### Task 14: Update Game Feature Files

**Files:**
- Modify: `frontend/src/features/game/commandDeck/viewModel.ts` (~282 Chinese lines — largest file)
- Modify: `frontend/src/features/game/decisionShared.ts` (~71 Chinese lines)
- Modify: `frontend/src/features/game/flow/decisionFlow.ts` (~18 Chinese lines)
- Modify: `frontend/src/features/game/flow/gameFlow.ts` (~58 Chinese lines)
- Modify: `frontend/src/features/game/flow/gameWorkbench.ts` (~116 Chinese lines)
- Modify: `frontend/src/features/game/flow/settlementFlow.ts` (~34 Chinese lines)
- Modify: `frontend/src/features/game/flow/useMapViewState.ts` (~18 Chinese lines)
- Modify: `frontend/src/features/game/forms.ts` (~39 Chinese lines)
- Modify: `frontend/src/features/game/runtime/useGameRuntime.ts` (~3 Chinese lines)

- [ ] **Step 1: Update commandDeck/viewModel.ts**

This is the file with the most Chinese text. Import `i18n` from `"../../../i18n"`. Replace all hardcoded Chinese strings throughout — these are primarily game action descriptions, status labels, and helper text. Most map to game concept labels already covered by game.json; remaining UI text goes into common.json or pages.json as appropriate.

- [ ] **Step 2: Update decisionShared.ts**

Replace Chinese strings. Import `i18n` from `"../../i18n"`.

- [ ] **Step 3: Update remaining feature files**

Replace all Chinese text in each file. Import `i18n` from the appropriate relative path.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/features/game/
git commit -m "feat: i18n for game feature files"
```

---

### Task 15: Update Remaining Frontend Files

**Files:**
- Modify: `frontend/src/app/AppShell.tsx` (~18 Chinese lines)
- Modify: `frontend/src/components/flow/FlowTaskList.tsx` (~4 Chinese lines)
- Modify: `frontend/src/components/flow/PageStatusBanner.tsx` (~1 Chinese line)
- Modify: `frontend/src/components/game/feedback/UnifiedSubmitPanel.tsx` (~9 Chinese lines)
- Modify: `frontend/src/components/game/layout/GameCommandDock.tsx` (~5 Chinese lines)
- Modify: `frontend/src/components/game/layout/GameMapView.tsx` (~1 Chinese line)
- Modify: `frontend/src/components/game/layout/GameTaskStrip.tsx` (~1 Chinese line)
- Modify: `frontend/src/components/game/map/GameMapModal.tsx` (~1 Chinese line)
- Modify: `frontend/src/components/game/map/PhaseAnnounce.tsx` (~5 Chinese lines)
- Modify: `frontend/src/components/game/status/GameSituationSummary.tsx` (~17 Chinese lines)
- Modify: `frontend/src/components/game/status/GameSubmissionPanel.tsx` (~12 Chinese lines)
- Modify: `frontend/src/features/flow/routeFlow.ts` (~23 Chinese lines)
- Modify: `frontend/src/features/game/demo/decisionCardDemo.ts` (~10 Chinese lines)
- Modify: `frontend/src/features/game/demo/seed.ts` (~106 Chinese lines)
- Modify: `frontend/src/test/gameSnapshotFixtures.ts` (~73 Chinese lines)

- [ ] **Step 1: Bulk update all remaining files**

For each file, add the i18n import and replace hardcoded Chinese strings with `t()` calls.

- [ ] **Step 2: Commit**

```bash
git add frontend/src/
git commit -m "feat: i18n for remaining frontend components"
```

---

### Task 16: Backend Presentation Config Language Variants

**Files:**
- Create: `backend/config/presentation/phases.en.json`
- Create: `backend/config/presentation/phases.zh.json`
- Create: `backend/config/presentation/countries.en.json`
- Create: `backend/config/presentation/countries.zh.json`
- Modify: `backend/app/modules/presentation_config.py`
- Modify: `backend/config/presentation/phases.json` → rename/remove
- Modify: `backend/config/presentation/countries.json` → rename/remove

- [ ] **Step 1: Create English phase descriptions**

Write `backend/config/presentation/phases.en.json` with English translations of the existing Chinese phase descriptions:

```json
{
  "phases": {
    "production": {
      "phaseRole": "The Production phase is where you turn budgets, inventory, and production lines into industrial output ready for the next round—goods to sell, upgrade, and expand.",
      "whyNow": "This is the starting point of the entire economic cycle. You're not making money yet—you're deciding how much inventory, how many production lines, and how much growth potential you'll have when this round ends.",
      "loopPositionText": "The full cycle is Production → Market → Military/Politics → influencing the next round's operations. You're now at step one: preparing goods and production lines."
    },
    "market": {
      "phaseRole": "The Market phase is where you convert existing inventory into revenue and channel this round's results back into consumption, investment, and fiscal pools.",
      "whyNow": "Goods prepared in the previous phase only become spendable funds in the consumption, investment, and fiscal pools once sold here.",
      "loopPositionText": "The full cycle is Production → Market → Military/Politics → influencing the next round's operations. You're now at the step that directly realizes revenue."
    },
    "military": {
      "phaseRole": "The Military phase determines naval power, regional control, and sea lane security—results that directly affect future market access and operational space.",
      "whyNow": "This isn't just combat—it's about whether you can safely sell goods next round, access critical regions, and whether someone can cut your economic chain.",
      "loopPositionText": "The full cycle is Production → Market → Military/Politics → influencing the next round's operations. You're now paving the way for the next round's market and regional operations."
    },
    "politics": {
      "phaseRole": "The Politics phase adjusts administration, shapes ideology, pushes reforms and research—giving the nation more stable operating conditions in subsequent rounds.",
      "whyNow": "This determines how the state machine runs. Administration, policies, reforms, and research together decide how money flows, what technology unlocks, and where risks emerge in the coming rounds.",
      "loopPositionText": "The full cycle is Production → Market → Military/Politics → influencing the next round's operations. You're now setting the tone for the next several rounds of operational efficiency and institutional conditions."
    }
  }
}
```

- [ ] **Step 2: Create zh variant**

Copy existing `phases.json` to `phases.zh.json`.

- [ ] **Step 3: Create country configs**

Create `countries.en.json` with English translations of country context descriptions, and `countries.zh.json` from existing `countries.json`.

- [ ] **Step 4: Update presentation_config.py**

Modify `backend/app/modules/presentation_config.py`:

```python
from __future__ import annotations

import json
from functools import lru_cache
from pathlib import Path
from typing import Any

from flask import request

DEFAULT_PRESENTATION_CONFIG_DIR = Path(__file__).resolve().parents[2] / "config" / "presentation"
GROUP_FILE_NAMES: tuple[str, ...] = (
    "phases",
    "countries",
)
DEFAULT_LANG = "zh"


def _resolve_lang() -> str:
    try:
        header = request.headers.get("Accept-Language", "")
        if header:
            primary = header.split(",")[0].split(";")[0].strip().lower()
            if primary in ("en", "zh"):
                return primary
    except RuntimeError:
        pass
    return DEFAULT_LANG


def load_presentation_config(config_dir: str | Path = DEFAULT_PRESENTATION_CONFIG_DIR) -> dict[str, Any]:
    resolved_dir = Path(config_dir).resolve()
    return _load_presentation_config_cached(str(resolved_dir))


def get_presentation_config(config_dir: str | Path | None = None) -> dict[str, Any]:
    if config_dir is None:
        return load_presentation_config(DEFAULT_PRESENTATION_CONFIG_DIR)
    return load_presentation_config(config_dir)


@lru_cache(maxsize=8)
def _load_presentation_config_cached(config_dir: str) -> dict[str, Any]:
    lang = _resolve_lang()
    resolved_dir = Path(config_dir)
    return {
        group_name: _read_group_json(resolved_dir, group_name, lang)
        for group_name in GROUP_FILE_NAMES
    }


def _read_group_json(config_dir: Path, group_name: str, lang: str) -> dict[str, Any]:
    group_path = config_dir / f"{group_name}.{lang}.json"
    if not group_path.exists():
        group_path = config_dir / f"{group_name}.json"
    payload = json.loads(group_path.read_text(encoding="utf-8"))
    if not isinstance(payload, dict):
        raise ValueError(f"Presentation config group must be a JSON object: {group_path}")
    return payload
```

- [ ] **Step 5: Commit**

```bash
git add backend/config/presentation/ backend/app/modules/presentation_config.py
git commit -m "feat: backend presentation config language variants with Accept-Language"
```

---

### Task 17: Backend API Messages i18n

**Files:**
- Create: `backend/app/i18n.py`
- Modify: `backend/app/api/routes.py` (with Chinese text)
- Modify: `backend/app/modules/game_state/workspaces.py` (with Chinese text)
- Modify: `backend/app/routes/settings.py` (with Chinese text)
- Modify: `backend/app/modules/settlement/final_result.py` (with Chinese text)
- (and other backend files with Chinese text)

- [ ] **Step 1: Create backend i18n module**

Write `backend/app/i18n.py`:

```python
from __future__ import annotations

from flask import request

STRINGS: dict[str, dict[str, str]] = {
    "en": {
        "session_not_found": "Session not found.",
        "room_not_found": "Room not found.",
        "player_not_in_room": "Player not in this room.",
        "game_already_started": "Game has already started.",
        "invalid_country": "Invalid country selection.",
        "country_taken": "This country is already taken.",
        "not_enough_players": "Not enough players to start.",
        "room_full": "Room is full.",
        "submission_deadline_passed": "Submission deadline has passed.",
        "invalid_phase": "Invalid game phase.",
        "config_load_error": "Failed to load configuration.",
        "config_save_error": "Failed to save configuration.",
        "config_save_success": "Configuration saved successfully.",
    },
    "zh": {
        "session_not_found": "未找到会话。",
        "room_not_found": "未找到房间。",
        "player_not_in_room": "玩家不在该房间中。",
        "game_already_started": "游戏已经开始。",
        "invalid_country": "无效的国家选择。",
        "country_taken": "该国已被选择。",
        "not_enough_players": "玩家数量不足，无法开始。",
        "room_full": "房间已满。",
        "submission_deadline_passed": "提交截止时间已过。",
        "invalid_phase": "无效的游戏阶段。",
        "config_load_error": "加载配置失败。",
        "config_save_error": "保存配置失败。",
        "config_save_success": "配置保存成功。",
    },
}


def t(key: str, **kwargs: object) -> str:
    try:
        header = request.headers.get("Accept-Language", "")
        lang = header.split(",")[0].split(";")[0].strip().lower() if header else "en"
    except RuntimeError:
        lang = "en"

    strings = STRINGS.get(lang, STRINGS["en"])
    message = strings.get(key, key)
    if kwargs:
        return message.format(**kwargs)
    return message
```

- [ ] **Step 2: Update backend files with Chinese text**

Replace hardcoded Chinese strings in backend files with `from app.i18n import t` and `t("key")` calls.

- [ ] **Step 3: Commit**

```bash
git add backend/app/i18n.py backend/app/
git commit -m "feat: backend API message i18n with Accept-Language support"
```

---

### Task 18: Add Language Switcher to SettingsPage

**Files:**
- Modify: `frontend/src/pages/SettingsPage.tsx`

- [ ] **Step 1: Add language toggle**

Import `useTranslation` from react-i18next. Add a language toggle component at the top of the SettingsPage:

```tsx
import { useTranslation } from "react-i18next";

// Inside SettingsPage component, add:
const { t, i18n } = useTranslation("pages");

// Language toggle section
<div style={{ marginBottom: "20px" }}>
  <span style={{ marginRight: "10px" }}>Language / 语言:</span>
  <button
    onClick={() => i18n.changeLanguage("en")}
    style={{
      fontWeight: i18n.language === "en" ? "bold" : "normal",
      marginRight: "8px",
      padding: "4px 12px",
      border: i18n.language === "en" ? "2px solid var(--color-accent)" : "1px solid #888",
      borderRadius: "4px",
      background: i18n.language === "en" ? "var(--color-accent-light, #e8f0fe)" : "transparent",
      cursor: "pointer",
    }}
  >
    English
  </button>
  <button
    onClick={() => i18n.changeLanguage("zh")}
    style={{
      fontWeight: i18n.language === "zh" ? "bold" : "normal",
      padding: "4px 12px",
      border: i18n.language === "zh" ? "2px solid var(--color-accent)" : "1px solid #888",
      borderRadius: "4px",
      background: i18n.language === "zh" ? "var(--color-accent-light, #e8f0fe)" : "transparent",
      cursor: "pointer",
    }}
  >
    中文
  </button>
</div>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/SettingsPage.tsx
git commit -m "feat: add language switcher to settings page"
```

---

### Task 19: Verify Build and Tests

- [ ] **Step 1: Install frontend dependencies and run type check**

Run: `cd frontend && npm install && npx tsc -b --noEmit`
Expected: No type errors

- [ ] **Step 2: Run frontend tests**

Run: `cd frontend && npm test`
Expected: All tests pass

- [ ] **Step 3: Run backend tests**

Run: `cd backend && python -m pytest tests/ -x --timeout=60 2>&1 | tail -20`
Expected: All tests pass

- [ ] **Step 4: Commit any fixes if needed**

If tests reveal issues, fix and commit separately.

---

### Task 20: Manual Verification Checklist

- [ ] **Step 1: Start dev server**

Run: `cd backend && python run.py`
Run: `cd frontend && npm run dev`

- [ ] **Step 2: Verify language detection**

Open the app in browser. If browser language is Chinese, app should display in Chinese; otherwise in English.

- [ ] **Step 3: Verify language switcher**

Go to Settings page, toggle language to Chinese and back to English. Verify text updates without page reload.

- [ ] **Step 4: Verify key pages**

Navigate through Home → Lobby → Room → Game → Settlement and verify all text displays correctly in both languages.

- [ ] **Step 5: Verify game panels**

Inside a game, check all panels (Factory, Market, Government, Military, Research, Domestic) display correctly in both languages.
