# 本轮修改计划与验证状态

> 更新时间：2026-05-21
> 定位：本文件只追踪本轮大改的执行状态。需求依据看 `docs/本次核心修改文档.md`，公式来源看 `docs/用户原始需求-核心机制.md`，旧资料只看归档。

## 状态口径

| 状态 | 含义 | 使用规则 |
| --- | --- | --- |
| 已验证 | 已在本轮最新文档或代码状态下完成对应验证，有明确命令或浏览器证据。 | 可以作为当前基线继续开发。 |
| 已开发未验证 | 本地代码或文档已经完成，但还没有在最新状态下跑完要求的测试或真实页面复验。 | 不能推送或对外宣称完成，下一步优先补验证。 |
| 待做 | 尚未实现，或现有实现明显不符合本轮口径。 | 需要开发拆分和测试。 |
| 待确认 | 需求口径仍缺客户/策划补充，当前只能按暂定方案规划。 | 可以先做工程保护，但不要写死成最终规则。 |

## 总览

| 模块 | 当前状态 | 当前结论 | 下一步 |
| --- | --- | --- | --- |
| 文档入口与旧文档归档 | 已验证 | 旧文档已合并到归档，当前入口已收敛。 | 后续新增结论只写入当前入口，不恢复旧阶段文档。 |
| 核心修改文档 | 已验证 | 最新 DOCX 已转换为 `docs/本次核心修改文档.md`，并同步 confirmation 副本。 | 后续客户补市场公式时继续更新该文档。 |
| 海外固定价格 | 已验证 | 海外价格口径已改为区域固定价，政策和竞争不再加价；自动化市场测试已覆盖固定价收入。 | 后续补真实页面出售验证。 |
| 单一海外容量政策 | 已验证 | 政府侧海外扩容集中为 `贸易促进`；`海军演练`已从当前玩家动作中删除，军事侧不再提供海外扩容动作。 | 后续补真实页面政策验证。 |
| 市场竞争额外容量 | 已验证 | 市场竞争只加额外可售容量，默认成功额外 +2，不加价格。 | 后续补真实页面竞争成功验证。 |
| 删除建交逻辑 | 已验证 | 海外区域默认开放，出售和竞争不再要求建交；自动化提交/市场测试已覆盖。 | 后续补真实页面无建交出售验证。 |
| 封锁航线解释 | 已开发未验证 | 玩家入口改为点开海外地区后分配舰队封锁该地区，并显示被封锁区域。 | 用真实页面验证名称、原因、影响、解除说明是否能看懂。 |
| 工厂总池与闲置工厂 | 已开发未验证 | 各国有总工厂池，`idle` 从总池计算并参与需求。 | 跑工厂规则测试和浏览器工厂页验证。 |
| 工厂升级 | 已开发未验证 | 升级是逐级转换，不增加总工厂数，成本低于扩建。 | 验证不能跳级、预算扣除、同回合生产可用。 |
| 工厂扩建 | 已开发未验证 | 每类型扩建次数上限和单个行业最终上限都已删除；扩建消耗 `idle`、预算，并受科技前置限制。 | 验证不限次数但不突破国家总上限，成本为 13 / 26 / 40 / 56。 |
| 材料购买 | 已开发未验证 | 材料按数量购买，受国家每回合上限限制，只影响本回合可用材料。 | 验证预算不足、国家上限、购买后投料立即生效。 |
| 政府财政单池 | 已开发未验证 | 不再使用 `政策专项额度 +8`，政策、研究、军事和行政力购买都走政府财政。 | 验证政府、研究、军事页面没有旧双池文案或错误预算来源。 |
| 研究周边逻辑 | 已开发未验证 | 当前文档口径为研究设施、研究进展、首研突破和后发直解锁。 | 在市场重构前后复查研究费用、结算反馈和页面文案。 |
| 市场重构开工逻辑 | 已验证 | 已根据核心修改文档和用户原始需求整理出可开工公式，见 `docs/plans/2026-05-21-市场重构开工逻辑整理.md`。 | 后续客户补公式时再更新。 |
| 国内市场软上限 | 已验证 | 国内投放只受库存约束，`K` 只作为定价软上限；`P=clamp(P0×(2-Q/K)+价格加成, 0.1×P0, 2×P0)` 已落地。 | 后续补真实页面超额投放验证。 |
| 市场收入分账 | 已验证 | 销售收入恢复按 3:3:4 回流民间购买力、工厂预算、政府财政，消费池不再冻结。 | 后续补真实页面结算页和下一回合资源验证。 |
| 市场预览和解释 | 已验证 | 市场面板显示定价池、定价软上限、动态最低价、动态最高价、短缺/过剩、预计收入；前端预览与后端公式共用同口径。 | 后续补真实页面可读性验证。 |
| Setting 调参入口 | 已开发未验证 | setting 公式说明已改为当前市场公式；配置层仍保留旧 `priceCeiling` 兼容字段但不参与 Phase 1 结算。 | 后续检查 settings 后端编辑项和真实页面说明。 |
| 最新真实浏览器 1-15 验证 | 待做 | 旧 1-15 记录早于本轮大改，不能覆盖当前状态。 | 按 `docs/playtests/QA_SESSION_PROTOCOL.md` 重新跑。 |
| 市场详细公式客户确认 | 待确认 | 确认书仍标注市场详细规则待客户补充。当前按原始需求和暂定工程保护执行。 | 客户补充后更新核心修改文档和本状态表。 |

## 开发顺序

1. 先补齐本轮已开发未验证项的自动化测试，避免在市场大重构时把旧问题混进去。
2. 补真实页面关键流程验证：市场超额投放、海外固定价、封锁解释、结算 3:3:4 回流。
3. 清理所有旧口径残留：每类型扩建次数、政策专项额度、殖民/征服主线文案。
4. 完成 setting 调参入口的真实页面验证。
5. 跑最新 1-15 真实浏览器流程。

## 必跑验证

### 文档级

- `git diff --check`
- 检查 `docs/README.md`、`docs/修改索引.md`、本文件和当前规划中的相对链接。
- `find docs -maxdepth 2 -type f | sort`，确认旧文档仍然收敛在 archive。

### 后端

- `backend/.venv/bin/python -m pytest backend/tests/test_balance_config.py backend/tests/test_rules_market.py backend/tests/test_rules_decision.py backend/tests/test_game_state_workspaces.py backend/tests/test_phase_submit_api.py -q`
- 已跑：`backend/.venv/bin/python -m pytest backend/tests/test_balance_config.py backend/tests/test_phase1_economy.py backend/tests/test_rules_market.py backend/tests/test_rules_settlement.py backend/tests/test_phase_submission_services.py backend/tests/test_phase_submit_api.py backend/tests/test_rules_v2_features.py backend/tests/test_m3_e2e_phase1_api.py backend/tests/test_m4_workspace_enrich.py backend/tests/test_m4_pipeline_mirror.py -q`，结果 `105 passed, 7 skipped`。

### 前端

- `npm test -- Phase1MarketPanel Phase1ProductionPanel FactoryPanel GamePhasePanelContent gameWorkbench decisionShared index`
- `npm run build`
- 已跑：`npm test -- Phase1MarketPanel GamePhasePanelContent decisionShared gameWorkbench index`，结果 `39 passed, 6 skipped`。
- 已跑：`npm run build`，通过。
- 已跑：`git diff --check`，通过。

### 真实浏览器

- 海外固定价：欧洲 > 美洲 > 亚太 > 中东 > 非洲，收入等于成交量乘固定价。
- `贸易促进` 只增加海外容量，不增加海外价格。
- 军事页不再出现 `海军演练`，只保留征募陆军、建造舰队和地区舰队部署。
- 市场竞争成功只增加额外可售容量。
- 无建交状态下可以出售和竞争海外区域。
- 被封锁地区显示封锁区域名、影响和解除方式。
- 工厂扩建消耗闲置名额，本回合生效，不消耗每类型扩建次数，也不受单个行业上限限制。
- 工厂升级逐级转换，不能跳级。
- 材料购买受国家上限和预算限制，购买后可立即投料。
- 国内市场可超软上限投放，单价下降且不出现负价格。

## 当前风险

- 不能把旧 playtest 的“无 P0 阻断”当成本轮完成证明，因为海外、工厂、财政和文档入口都已经变动。
- 客户已补充国内价格曲线和价格下限/上限；后续若再调整分账或海外价格表，需要同步更新公式文档和页面说明。
- 旧测试 fixture 很可能还保留 `rewardPriceBonus`、`overseasPriceBonusDelta`、`established_diplomacy`、工厂扩建次数等字段，需要集中清理。
- 浏览器页面上的玩家文案比后端字段更容易暴露残留，例如“建交”“海外价格 +N”“政策专项额度”“殖民/征服收益”。
